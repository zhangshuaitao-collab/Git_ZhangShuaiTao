# 自动点击工具

一个简单的桌面自动点击工具，支持：
- 选择点击位置
- 设置点击间隔范围（毫秒）
- 设置点击次数与点击类型

## 运行

1. 安装依赖：
```
pip install -r requirements.txt
```

2. 启动：
```
python main.py
```

## 使用说明

- 点击“选择位置(3秒)”后，3秒内把鼠标移到目标位置
- 或者点击“使用当前鼠标位置”立即捕获
- 设置最小/最大间隔后点击“开始”
- 点击“停止”结束
- 全局快捷键：`Ctrl+T` 停止（无需窗口焦点）

